<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <!-- namespace：该mapper.xml映射文件的唯一标识 -->
<mapper namespace="person.demo.mapper.StudentMapper">
	<!-- parameterType：输入类型参数
			resultType：返回类型
	-->
	<select id="queryStudentByStuno"	parameterType="int"	 resultType="person.demo.entity.Student">
		select * from student where stuno = #{stuno}
	</select>
	
	<insert id="addStudent" parameterType="person.demo.entity.Student" >
		insert into student (stuno , stuname , stuage , graname) values ( #{stuNo} , #{stuName} , #{stuAge} , #{graName} );
	</insert>
	
	<delete id="deleteStudentByStuno"	parameterType="int">
		delete from student where stuno = #{stuno}
	</delete>
	
	<update id="updateStudentByStuno" parameterType="person.demo.entity.Student">
		update student set stuname = #{stuName}  , stuage = #{stuAge} , graname = #{graName} where stuno = #{stuNo}	
	</update>
	
	<select id="queryAllStudents" resultType="person.demo.entity.Student">
		select * from student
	</select>
</mapper>
